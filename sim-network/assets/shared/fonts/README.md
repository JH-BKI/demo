# Place shared offline font files here (e.g. .woff2).
# Reference them from JSON via general.typography / scenes[].typography
# with type "local" and files[].path like:
#   "./assets/shared/fonts/MyFont-Regular.woff2"
